* text=auto
*.rs text diff=rust
Cargo.lock linguist-generated=true
*.md text diff=markdown
*.toml text
*.sh text eol=lf

# Release artifacts and hermetic checksum fixtures must round-trip byte-exact.
*.wasm binary
