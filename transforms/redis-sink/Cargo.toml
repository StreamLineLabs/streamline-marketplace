[package]
name = "streamline-redis-sink"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sink connector that writes data to Redis via XADD, SET, or PUBLISH"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
