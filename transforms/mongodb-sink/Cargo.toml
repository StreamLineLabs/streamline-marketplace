[package]
name = "streamline-mongodb-sink"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sink connector that inserts or upserts data into MongoDB"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
