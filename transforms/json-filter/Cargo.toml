[package]
name = "json-filter"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM transform that filters messages by JSON field values"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
