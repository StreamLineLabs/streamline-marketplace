[package]
name = "streamline-http-webhook-sink"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sink connector that sends data via HTTP POST/PUT webhooks"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
