[package]
name = "streamline-elasticsearch-sink"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sink connector that bulk indexes data to Elasticsearch"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
