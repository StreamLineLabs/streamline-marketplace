[package]
name = "field-router"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM transform that routes messages to topics based on field values"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
