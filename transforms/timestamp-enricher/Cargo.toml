[package]
name = "timestamp-enricher"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM transform that adds processing timestamps to JSON messages"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
