[package]
name = "pii-redactor"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASM transform that detects and redacts PII from JSON messages"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"
regex = "1.10"
